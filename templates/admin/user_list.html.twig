{% extends "base.html.twig" %}

{% block title %}List Users{% endblock %}

{% block content %}
<h2>Users ({{ users|length }})</h2>
{% if users|length > 0 %}
<table>
<thead>
<tr>
<th>Name</th>
<th>Email</th>
</tr>
</thead>
<tbody>
{% for user in users %}
<tr>
<td>{{ user.displayName }}</td>
<td>{{ user.mail }}</td>
</tr>
{% endfor %}
</tbody>
</table>
{% endif %}
{% endblock %}
